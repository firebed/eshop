<meta property="og:image" content="{{ asset(eshop('logo.path')) }}">
<meta property="og:image:width" content="{{ eshop('logo.width') }}" />
<meta property="og:image:height" content="{{ eshop('logo.height') }}" />